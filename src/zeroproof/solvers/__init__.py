"""The eight-category solver suite.

Provable categories carry a proof (executed code, a second method that agrees,
a satisfied constraint model). Linguistic categories are governed by the local
LLM with deterministic format enforcement and safe fallbacks.
"""
