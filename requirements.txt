# ZeroProof runtime dependencies (CPU-only, linux/amd64).
# Deterministic solver stack — all permissively licensed (MIT/BSD/Apache).
spacy==3.8.14
click==8.4.2          # spaCy's CLI imports click directly; not always pulled transitively
sympy==1.14.0
python-constraint2==2.6.0
dateparser==1.4.1
numpy==2.4.6

# Local inference runtime (GGUF via llama.cpp). Installed from the prebuilt CPU
# wheel index in the Dockerfile so no compiler toolchain is needed at build time.
# Pinned to a version with a verified cp311 linux/amd64 CPU wheel.
llama-cpp-python==0.3.34
